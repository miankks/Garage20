@model IEnumerable<Garage20.Models.Medlem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Förnamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Efternamn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Förnamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Efternamn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MedlemsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MedlemsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MedlemsId })
        </td>
    </tr>
}

</table>
